eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://${eureka.instance.hostname}/${server.port}/eureka/
server:
  port: 8761